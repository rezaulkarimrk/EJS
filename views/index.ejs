<%- include("layouts/header")%>
    <main>

        <form action="/" method="post">
            <input type="text" placeholder="Programming Languages" name="pLanguages" />
            <button type="submit" >Add</button>
        </form>

        <ul>
            <% if(pNames.length >0){ %>
                <% for(let i = 0; i < pNames.length; i++){ %>
                <li><%= pNames[i] %></li>
                <% } %>
            <% } else { %>
                <p>No Data found</p>
            <% } %>
        </ul>

    </main>
<%- include("layouts/footer")%>